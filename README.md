# mattermost-_terraform
mattermost terraform script using terraform 
main.tf – Contains  main infrastructure code including resources like EC2, SG, VPC etc.

provider.tf – Defines the cloud provider - AWS.

variables.tf – Stores input variables.

outputs.tf – Exposes useful outputs like public IPs.
